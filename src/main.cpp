//#include <random>
//#include <iostream>
//#include "2D/algorithms_2D/SmallestEnclosingDisk.h"
//
//#define min 0
//#define max 1000
//#define reps 200
//
//using Point = Shapes2D::Point2d;
//using PointsVector = std::vector<Point>;

int main() {
//    auto util = Algorithms2d::SmallestEnclosingDisk();
//
//    std::random_device rd;     // Only used once to initialise (seed) engine
//    std::mt19937 rng(rd());    // Random-number engine used (Mersenne-Twister in this case)
//    std::uniform_int_distribution<int> uni(min, max); // Guaranteed unbiased
//
//    auto * points = new PointsVector();
//
//    for (int i=0; i<reps; ++i) {
//        points->emplace_back(uni(rng), uni(rng));
//    }
//
//    auto * circle = Algorithms2d::SmallestEnclosingDisk::findDisc(*points);
//    std::cout<<circle->getCenter()->toStr()<<std::endl;
//    return 0;
}